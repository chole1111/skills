# 测试用例生成助手

## 描述

根据需求描述或截图生成测试用例Excel文件。支持字段验证、列表功能、筛选导出、权限角色、数据关联等测试场景。输出格式：微软雅黑字体，8列格式（用例编号、所属模块、用例标题、用例优先级、前置条件、步骤描述、预期结果、执行结果）。

## 参数说明

- **argument-hint**: 编写测试用例、生成测试用例、测试用例编写

## 功能说明

### Excel格式规范
- **字体**：微软雅黑（Microsoft YaHei）
- **格式**：.xlsx
- **Sheet名**：测试用例
- **8列字段**：用例编号、所属模块、用例标题、用例优先级、前置条件、步骤描述、预期结果、执行结果

### 用法

直接根据用户提供的需求，编写测试用例并生成Excel文件。

**重要习惯：**
- 追加模式：在现有Excel文件中追加新用例，不删除已有用例
- 连续编号：在现有最大用例编号基础上递增

#### 模块命名规范
使用"|"分隔层级：
- 小程序端|农事任务|基地管理|培训
- web管理后台|基地管理|外来人员管理
- 交易小程序端|企业管理员

#### 优先级定义
| 优先级 | 说明 |
|--------|------|
| P0 | 核心功能，必须测试 |
| P1 | 重要功能 |
| P2 | 辅助功能 |
| P3 | 建议测试 |

### 常用测试点

#### 一、字段验证
- 必填字段验证（为空提示）
- 格式验证（手机号、身份证、邮箱、日期等）
- 长度限制（最小/最大字符数）
- 唯一性验证（重复提交提示）
- 空格/特殊字符处理
- 边界值验证（最大/最小值）

#### 二、增删改查
- 新增成功验证
- 新增失败验证（各种异常情况）
- 编辑保存验证
- 编辑后数据准确性
- 删除成功验证（含确认弹窗）
- 取消删除验证
- 删除后数据一致性
- 查询/搜索准确性
- 条件组合查询

#### 三、列表功能
- 列字段完整性
- 数据展示正确性
- 分页加载（首页/末页/指定页）
- 分页数据条数
- 排序功能（升序/降序）
- 多字段排序
- 筛选功能（单条件/多条件）
- 筛选结果准确性
- 重置筛选
- 空数据展示
- 数据刷新/更新

#### 四、导出功能
- 导出按钮存在性
- 全部导出
- 筛选导出
- 选中导出（部分导出）
- 导出文件格式（.xlsx/.csv）
- 导出数据准确性
- 导出数据完整性
- 空数据导出
- 大数据量导出

#### 五、权限验证
- 管理员vs普通用户
- 功能入口显隐
- 功能操作权限
- 数据查看权限
- 越权操作提示
- 登录状态验证
- 登出功能

#### 六、数据关联
- 新增后列表展示
- 新增后数据自动刷新
- 主子表数据一致性
- 跨模块数据同步
- 数据关联字段展示
- 级联操作验证

#### 七、界面/交互
- 页面加载速度
- 按钮可点击状态
- 输入框可编辑状态
- 弹窗显示/关闭
- 提示信息显示
- 加载状态显示
- 页面跳转
- 返回功能
- 刷新功能

#### 八、异常处理
- 网络异常提示
- 服务器错误处理
- 超时处理
- 数据异常处理
- 并发操作处理
- 重复操作处理

### 测试设计方法（提高覆盖度）

#### 1. 等价类划分
将输入数据分成有效等价类和无效等价类，每个等价类选取代表值测试。
- 有效等价类：符合业务规则的数据
- 无效等价类：不符合业务规则的数据

#### 2. 边界值分析
测试边界值及其相邻值，包括：
- 最小值/最大值
- 临界值（比最大值大1/小1）
- 空值/空字符串
- 唯一值/重复值

#### 3. 场景法
基于业务流程，设计完整的场景测试：
- 基本流（正常流程）
- 备选流（分支流程）
- 异常流（异常情况处理）

#### 4. 状态迁移
验证对象状态之间的转换：
- 状态定义
- 状态转换条件
- 状态转换路径
- 非法状态转换

#### 5. 因果图法
分析输入条件和输出结果之间的因果关系：
- 原因（输入条件）
- 结果（输出结果）
- 约束关系

#### 6. 错误推测法
基于经验推测可能存在的错误：
- 常见错误模式
- 历史bug回顾
- 边界条件遗漏

### Python生成脚本

```python
import openpyxl
from openpyxl.styles import Font, Alignment, Border, Side

wb = openpyxl.Workbook()
ws = wb.active
ws.title = '测试用例'

headers = ['用例编号', '所属模块', '用例标题', '用例优先级', '前置条件', '步骤描述', '预期结果', '执行结果']
ws.append(headers)

header_font = Font(name='微软雅黑', bold=True, size=11)
data_font = Font(name='微软雅黑', size=10)
thin_border = Border(left=Side(style='thin'), right=Side(style='thin'), top=Side(style='thin'), bottom=Side(style='thin'))

for cell in ws[1]:
    cell.font = header_font
    cell.border = thin_border
    cell.alignment = Alignment(horizontal='center', vertical='center', wrap_text=True)

test_cases = [
    ['TC_001', '模块路径', '用例标题', 'P0', '前置条件', '步骤描述', '预期结果', ''],
]

for row in test_cases:
    ws.append(row)

ws.column_dimensions['A'].width = 12
ws.column_dimensions['B'].width = 30
ws.column_dimensions['C'].width = 28
ws.column_dimensions['D'].width = 12
ws.column_dimensions['E'].width = 35
ws.column_dimensions['F'].width = 45
ws.column_dimensions['G'].width = 45
ws.column_dimensions['H'].width = 12

for row in ws.iter_rows(min_row=2, max_row=ws.max_row):
    for cell in row:
        cell.font = data_font
        cell.border = thin_border
        cell.alignment = Alignment(vertical='top', wrap_text=True)

wb.save('测试用例.xlsx')
```

## 使用示例

根据用户需求生成测试用例：
- 模块：交易小程序端-企业管理员
- 功能点：新增成员
- 测试点：姓名必填、手机号必填、手机号唯一性

生成测试用例并保存到Excel文件。

---

## 补充说明

## 补充说明

### 测试覆盖率原则

1. **功能覆盖**：每个功能点都有测试用例
2. **路径覆盖**：关键业务流程都有测试
3. **边界覆盖**：边界值都有测试
4. **异常覆盖**：异常情况都有测试
5. **精简覆盖**：用最少的用例覆盖最全的测试点

### 用例设计原则

- 原子性：每个用例只测试一个功能点
- 可执行性：步骤描述清晰可执行
- 可验证性：预期结果明确可判断
- 独立性：用例之间无依赖关系
- **精简性：用最少的用例覆盖最多的测试点，避免冗余**

### 精简原则（避免冗余）

1. **功能合并原则**
   - 相似的验证点可以合并到一个用例中
   - 例如："姓名必填"和"手机号必填"可以合并为一个用例"新增成员-必填字段验证"

2. **优先级筛选**
   - P0（核心功能）必写
   - P1（重要功能）选写
   - P2/P3（非关键）不写或合并写

3. **等价类合并**
   - 同一个字段的多个无效等价类，选1-2个典型测试即可
   - 不需要每个边界值都写一个用例

4. **场景简化**
   - 基本流程必测
   - 分支流程选择性测试
   - 异常流程只测试关键异常

5. **不做重复测试**
   - 已有的测试点不重复编写
   - 类似的操作只写一个典型用例

### 精简示例

**冗余写法（20条）：**
- TC_001: 新增-姓名必填验证
- TC_002: 新增-手机号必填验证
- TC_003: 新增-姓名空格验证
- TC_004: 新增-手机号空格验证
- TC_005: 新增-姓名长度10验证
- TC_006: 新增-手机号长度11验证
- ...

**精简写法（8条）：**
- TC_001: 新增成员-必填字段验证（姓名、手机号都为空）
- TC_002: 新增成员-格式验证（手机号格式错误）
- TC_003: 新增成员-格式验证（身份证格式错误）
- TC_004: 新增成员-长度限制（姓名为空、正常、超长）
- TC_005: 新增成员-唯一性验证（手机号已存在）
- TC_006: 新增成员-正常流程（所有字段正确）
- TC_007: 编辑成员-保存成功
- TC_008: 删除成员-确认删除

### 常见功能测试点速查表

| 功能类型 | 必须测试点 | 建议测试点 |
|---------|-----------|-----------|
| 输入框 | 必填、空格、格式、长度、特殊字符 | 粘贴、复制、撤销 |
| 按钮 | 可点击/不可点击、点击反馈 | 双击、快捷键 |
| 下拉框 | 选择、搜索、清空 | 多选、级联 |
| 表格 | 列展示、分页、排序、筛选 | 固定列、滚动加载 |
| 弹窗 | 显示、关闭、确认、取消 | 遮罩点击、ESC关闭 |
| 上传 | 文件类型、大小、数量 | 拖拽、批量上传 |
| 导出 | 全部、部分、筛选 | 进度条、下载失败 |

---
*根据实际需求生成标准化测试用例Excel文件*